@page
@model Avenue804.Web.Areas.Admin.Pages.SiteSettings.IndexModel
@{
    ViewData["Title"] = "Site settings";
}
<p class="muted admin-lead-muted">Values here override the <code>Site</code> section in appsettings. Clear or edit to change public contact details and branding.</p>
<p class="admin-back admin-back--tight"><a asp-page="Create">+ Add setting</a></p>
<div class="admin-table-wrap">
  <table class="admin-table">
    <thead><tr><th>Key</th><th>Value</th><th></th></tr></thead>
    <tbody>
    @foreach (var r in Model.Rows)
    {
      <tr>
        <td><code>@r.Key</code></td>
        <td class="admin-td-clip">@(r.Value.Length > 120 ? r.Value[..120] + "…" : r.Value)</td>
        <td><a asp-page="Edit" asp-route-id="@r.Id">Edit</a></td>
      </tr>
    }
    </tbody>
  </table>
</div>
@if (Model.Rows.Count == 0)
{
  <p class="muted">No database overrides yet — the site uses appsettings only.</p>
}
